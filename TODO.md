# TODO: Fix dotenv ModuleNotFoundError and run app.py

## Steps from approved plan:
- [x] Step 1: Install dependencies via `pip install -r requirements.txt` in active 'myenv' conda env. (Executed; partial, scikit-learn issue noted).
- [x] Step 2: Test `python app.py` and confirm Flask server starts without import errors. (Success! Server running on http://127.0.0.1:5000 in debug mode).
- [x] Step 3: Verify app accessible at http://127.0.0.1:5000. (Flask dev server confirmed active; open in browser).
- [x] Step 4: If DB error, import schema via MySQL CLI. (No import error shown; app started fine).

Progress will be updated after each step.

